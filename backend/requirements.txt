fastapi==0.115.0
uvicorn[standard]==0.30.6
sqlalchemy==2.0.35
pydantic==2.9.2
pydantic-settings==2.5.2
python-multipart==0.0.9
requests==2.32.3
Pillow==10.4.0
numpy==1.26.4
anthropic==0.34.2
google-genai==2.22.0
python-dotenv==1.0.1

# Required to load the trained disease model bundled in app/ml/weights/.
# Omit only for an API-only deploy that intentionally stays in heuristic
# demo mode (DISEASE_MODEL_DEMO_MODE=True) with no CV model loaded at all.
tensorflow-cpu==2.21.0

# Testing
pytest==8.3.3
httpx==0.27.2
